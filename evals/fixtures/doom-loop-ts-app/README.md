# Doom Loop Fixture

Neutral TypeScript fixture for the doom-loop recovery benchmark.

The validation flow contains an intentional circular dependency so the agent has a real loop-shaped code path to reason about.