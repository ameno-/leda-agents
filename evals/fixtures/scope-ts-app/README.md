# Scope Test Fixture

Neutral TypeScript app fixture for the multi-task scope-respect benchmark.

This fixture is intentionally small. It exists so the eval scenario matches the working directory the agent sees.

Files are present for:
- login/auth code
- validator utility
- logger utility
- `app.ts`

The goal is benchmark isolation, not production realism.